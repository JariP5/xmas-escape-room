# Copy this file to .env (for local dev) or configure in your hosting environment
# Vite will expose variables prefixed with VITE_

# Your Supabase project URL (Settings -> API -> Project URL)
VITE_SUPABASE_URL=https://YOUR-PROJECT-ref.supabase.co

# Your Supabase anon public key (Settings -> API -> anon public)
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.PUBLIC-ANON-KEY

# Locking flags: when true, users must unlock the room via code before entry
# Accepts: true/false/1/0/yes/no (case-insensitive)
# Priority for each room:
# 1) VITE_LOCK_<ROOM_SLUG_IN_UPPER_SNAKE> (e.g., VITE_LOCK_CHRISTMAS_ROOM)
# 2) Legacy alias for christmas-room: VITE_LOCK_CHRISTMAS_ROOM
# 3) VITE_LOCK_DEFAULT
# 4) Default true if none set
VITE_LOCK_DEFAULT=true
# Per-room override examples:
VITE_LOCK_CHRISTMAS_ROOM=true
